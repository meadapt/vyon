[tool.taskipy.tasks]
devserver = { cmd = "python manage.py runserver", help = "Starts local dev django server." }
prodserver = { cmd = "python manage.py runserver", help = "Starts production django server." }
makemigrations = { cmd = "echo 'Rodando makemigrations' && python manage.py makemigrations", help = "Create migrations files." }
migrate = { cmd = "echo 'Rodando migrations' && python manage.py migrate", help = "Apply migrations to the db." }
erd = { cmd = "python scripts/erd.py", help = "Create ERD picture." }
